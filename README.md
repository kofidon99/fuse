# fuse
A merge puzzle about agreement. Same ranks fuse into one a rank higher — and it keeps the charge only if every piece that went into it agreed. Four of a charge in a run and the lot goes up. Single HTML file, no build step, no dependencies, no external network requests.
